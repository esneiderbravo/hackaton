## 1. Dependency Setup

- [x] 1.1 Add `d3`, `leaflet`, `react-leaflet`, and `@types/leaflet` to `package.json` dependencies.
- [x] 1.2 Install dependencies and update lockfile entries.

## 2. Verification

- [x] 2.1 Run `npm run lint` and confirm no new lint errors caused by this change.
- [x] 2.2 Run `npm run type-check` and confirm no new TypeScript errors caused by this change.
